## 一、基础知识选择题（1-10 题）

1. 下列语言中不属于面向过程的语言是 。 A. 高级语言 B. 低级语言 C. C 语言 D. PASCAL 语言
    
2. 下列那个是 4 的二进制 。 A. 10 B. 11 C. 100 D. 110
    
3. 计算机中信息存储的最小单位是 。 A. 位 B. 字长 C. 字 D. 字节
    
4. 计算机的 CPU 每执行一个 ，就完成一步基本运算或判断。 A. 语句 B. 指令 C. 程序 D. 软件
    
5. 计算机中的机器数有三种表示方法，下列 不是。 A. 反码 B. 原码 C. 补码 D. ASCII
    
6. 下列有关存储器读写速度的排列，正确的是 。 A. RAM&gt;Cache &gt; 硬盘 &gt; 软盘 B. Cache&gt;RAM &gt; 硬盘 &gt; 软盘 C. Cache &gt; 硬盘 &gt; RAM &gt; 软盘 D. RAM &gt; 硬盘 &gt; 软盘 &gt; Cache
    
7. 为了避免混淆，十六进制数在书写时常在后面加字母 。 A. H B. O C. D D. B
    
8. 将高级语言编写的程序翻译成机器语言程序，采用的两种翻译方式是 。 A. 编译和解释 B. 编译和汇编 C. 编译和链接 D. 解释和汇编
    
9. 操作系统的主要功能是 。 A. 实现软、硬件转换 B. 管理系统所有的软、硬件资源 C. 把源程序转换为目标程序 D. 进行数据处理
    
10. 与十六进制数（BD）等值的二进制数是 。 A. 10111011 B. 10111101 C. 11001100 D. 11001011
    
---
## 二、数据库题目（11-16 题）

11. 请分别说明数据库中常见的主键约束、外键约束、检查约束、唯一约束、默认约束。（10 分）
    
12. 有一个学生表，结构为：`tstudent\(id, name, course, score\)`，表中数据如下：
    
|id|name|course|score|
|---|---|---|---|
|1|学生 1|语文|88.50|
|2|学生 1|数学|95.00|
|3|学生 1|英语|85.50|
|4|学生 2|语文|100.00|
|5|学生 2|数学|76.00|
|6|学生 2|英语|70.50|
|7|学生 3|语文|68.50|
|8|学生 3|数学|77.50|
|9|学生 3|英语|91.50|
|10|学生 4|语文|72.50|
|11|学生 4|数学|86.50|
|12|学生 4|英语|77.50|

要求进行行转列，得出如下结果输出结果：

|name|语文|数学|英语|
|---|---|---|---|
|学生 2|100.00|76.00|70.50|
|学生 1|88.50|95.00|85.50|
|学生 4|72.50|86.50|77.50|
|学生 3|68.50|77.50|91.50|
|（10 分）||||

13. 关系模式：
    
- `User\(userId, userName\)`
    
- `Article\(articleId, userId, title, content\)`
    
- `Vote\(articleId, score\)`
    
`User`为用户关系，`Article`为用户发表的文章关系，`Vote`为文章得票关系，`title`为文章标题、`score`为得票数。 

13. 用 SQL 语言查询所有没发表过文章的用户名。（5 分）
	
14. 用 SQL 语言查询得票数大于 100 的所有文章标题，按得票数倒序排列。（5 分）
    
15. 用 SQL 语言查询出发表文章数大于 5，文章平均得票数大于 100 的用户名，按平均得票数倒序排列。（5 分）
	    SELECT u.userName FROM User AS u 
	    LEFT JOIN Article AS a ON u.userId = a.userId 
	    LEFT JOIN Vote AS v ON a.articleId = v.articleId 
	    GROUP BY u.userName 
	    HAVING COUNT(DISTINCT a.articleId) > 5 AND AVG(v.score) > 100 
	    ORDER BY AVG(v.score) DESC; 
16. 设计这些表的主键、外键和索引，并指出上面三个查询所使用的索引。（5 分）
    

---

## 三、Java 基础选择题（17-26 题）

17. Java 所有的类都是继承于 。 A. Object B. Class C. Parent D. Main
    
18. 以下对抽象类正确的描述是 。 
    A. 抽象类一定存在抽象方法 
    B. 存在抽象方法的一定是抽象类 
    C. 抽象类可以被实例化 
    D. 抽象类不可以被实例化
    
20. 运行以下代码之后，控制台输出结果是（ ）。
    
String s1 = "abc";  
String s2 = "abc";  
String s3 = new String("abc");  
System.out.print((s1 == s2) + "、");  
System.out.print((s1 == s3) + "、");  
System.out.print(s1.equals(s2) + "、");  
System.out.println(s1.equals(s3));

A. true、true、true、true 
B. true、false、true、true 
C. true、true、false、false 
D. true、false、true、false

20. 已知如下的命令执行 `java MyTest a b c`，请问哪个语句是正确的 ？ 
    A. args[0] = MyTest a b c
    B. args[0] = MyTest 
    **C. args[0] = a**
    D. args[1]= c
    
21. 以下哪个表达式是不合法的。
     A. `String x=Hello; int y=9; x+=y;` 
     **B. `String x=Hello; int y=9; if(x==y) { };`** ❌：二元运算符 ' == ' 的操作数类型错误
     C. `String x=Hello; int y=9; x=x+y;` 
     D. `String x=null; int y=(x!=null)&&(x.length()>0) ? x.length() : 0`
    
22. 以下声明合法的是 。 
    A. `default String s;` ❌: default用于接口中的方法，访问控制修饰符
    **B. `public final static native int w;`** ✔：`native`：表示该方法由本地代码（如 C/C++）实现
    C. `abstract double d;` ❌：abstract用于类和方法
    D. `abstract final double hyperbolicCosine().` 
    ❌：`abstract` 和 `final` 是**互斥的**修饰符
    
24. `Math.round(11.5)` 和 `Math.round(-11.5)` 分别等于
    A. 11 , -11 
    B. 11.5, -11.5 
    **C. 12, -12**
    D. 12, -11
    
25. 关于以下 application 的说明，正确的是 。
class StaticStuff  
{  
    static int x=10;  
    static { x+=5; }  
    public static void main(String args[ ])  
    {  
        System.out.println("x=" + x);  
    }  
    static { x/=3; }  
}

A. 4 行与 9 行不能通过编译，因为缺少方法名和返回类型； 
B. 9 行不能通过编译，因为只能有一个静态初始化器； 
**C. 编译通过，执行结果为：x=5;**
D. 编译通过，执行结果为：x=3.

25. 下面代码：
a=10,  
b=4,  
c=20,  
d=6;  
System.out.println(a++*b+c*--d);

A. 144 
B. 28 
**C. 140**
D. 不能执行

26. 运行以下代码结果是什么？
public class Test {  
    private int i = j;  
    private int j = 10;  
​  
    public static void main(String args[]) {  
        System.out.println((new Test()).i);  
    }  
}

A. 编译不通过，报引用 Test 的私有变量错误； 
**B. 编译不通过，报错误引用;**
C. 编译通过，输出结果 0; 
D. 编译通过，输出结果 10.
## 四、编程题（27 题）

27. 编写一个方法，通过二分查找，从一个有序数组中找到目标值。（Java 实现）
// 题目：编写方法，用二分查找，在一个有序数组当中找到目标值

public class  binarySearch{

    public int binarySearch(int[] arr, int target){
        int left = 0;
        int right = arr.length - 1;
        while(left <= right){
            int mid = left + (right - left) / 2;
            if(arr[mid] == target){
                return mid;
            }else if(arr[mid] < target){
                left = mid + 1;
            }else{
                right = mid - 1;
            }
        }
        return -1;}
    public static void main(String[] args) {
        int[] arr = {1,2,3,4,5,6,7,8,9,0};
        int target = binarySearch(arr, 5);
        System.out.println(target);
    }
}