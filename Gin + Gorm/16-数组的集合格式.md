1. 你可以使用表单绑定中的 `collection_format` 结构体标签来控制 Gin 如何拆分切片/数组字段的列表值。
2. 支持的格式
	- multi（默认）：重复键或逗号分隔的值
	- csv：逗号分隔的值
	- ssv：空格分隔的值
	- tsv：制表符分隔的值
	- pipes：管道符分隔的值
3. 集合的默认值：
	- 在 `form` 标签中使用 `default` 来设置回退值。
	- 对于 `multi` 和 `csv`，使用分号分隔默认值：`default=1;2;3`。
	- 对于 `ssv`、`tsv` 和 `pipes`，在默认值中使用自然分隔符。